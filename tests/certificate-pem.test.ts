import assert from 'node:assert/strict'; import test from 'node:test'; import { validatePemBundle } from '../src/runtime/certificate-pem'; test('validates certificate PEM marker', () => assert.doesNotThrow(() => validatePemBundle({ certificatePem: '-----BEGIN CERTIFICATE-----x' }))); test('rejects malformed certificate', () => assert.throws(() => validatePemBundle({ certificatePem: 'bad' }), /certificate_pem_invalid/));
