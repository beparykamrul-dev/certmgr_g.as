export interface PrefixRecord { prefix: string; asn: number; source: string; observedAt: string; verified: boolean; }
export interface AsnRecord { asn: number; name?: string; country?: string; source: string; observedAt: string; }
