import type { Migration } from './migration-runner';

export const RUNTIME_MIGRATIONS: readonly Migration[] = [
  { version: '001_approval_audit', sql: `CREATE TABLE IF NOT EXISTS approval_requests (id UUID PRIMARY KEY, action TEXT NOT NULL, target TEXT NOT NULL, requested_by TEXT NOT NULL, state TEXT NOT NULL CHECK (state IN ('pending','approved','rejected','expired','executed')), created_at TIMESTAMPTZ NOT NULL, expires_at TIMESTAMPTZ); CREATE INDEX IF NOT EXISTS approval_requests_state_idx ON approval_requests(state); CREATE INDEX IF NOT EXISTS approval_requests_created_at_idx ON approval_requests(created_at DESC); CREATE TABLE IF NOT EXISTS audit_records (id UUID PRIMARY KEY, action TEXT NOT NULL, actor TEXT NOT NULL, outcome TEXT NOT NULL CHECK (outcome IN ('approved','denied','executed','failed')), target TEXT, timestamp TIMESTAMPTZ NOT NULL, request_id UUID); CREATE INDEX IF NOT EXISTS audit_records_timestamp_idx ON audit_records(timestamp DESC); CREATE INDEX IF NOT EXISTS audit_records_request_id_idx ON audit_records(request_id);` },
  { version: '002_certificate_inventory', sql: `CREATE TABLE IF NOT EXISTS certificate_inventory (subject TEXT PRIMARY KEY, issuer TEXT, expires_at TIMESTAMPTZ NOT NULL, source TEXT NOT NULL, observed_at TIMESTAMPTZ NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()); CREATE INDEX IF NOT EXISTS certificate_inventory_expires_idx ON certificate_inventory(expires_at);` },
];
